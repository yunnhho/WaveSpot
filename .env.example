# ============================================
# WAVESPOT 루트 환경변수 템플릿 (docker-compose 전용)
# cp .env.example .env
# API 앱 환경변수는 apps/api/.env.example 참조
# ============================================

# PostgreSQL (docker-compose에서 사용)
POSTGRES_USER=wavespot
POSTGRES_PASSWORD=wavespot_dev_2026
POSTGRES_DB=wavespot
